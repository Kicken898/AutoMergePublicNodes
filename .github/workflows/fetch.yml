name: Fetch Nodes

on:
  schedule:
    - cron: '0 */12 * * *'   # 每12小时运行一次
  workflow_dispatch:          # 允许手动触发

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run fetch script
        env:
          NO_PROXY: ${{ secrets.NO_PROXY }}        # 可选，部分配置需要
          HTTP_PROXY: ${{ secrets.HTTP_PROXY }}     # 可选，如需代理可设置
          HTTPS_PROXY: ${{ secrets.HTTPS_PROXY }}   # 可选
        run: python fetch.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add list.txt list.yml list.meta.yml list_raw.txt list.result.csv
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto update nodes"
          git push
